# Development-only Groq proxy. Never prefix this key with VITE_.
GROQ_API_KEY=
# Optional secure external production proxy; blank means Demo Mode in production.
VITE_AUDIT_API_URL=
# Works at both a root domain and /assignment-audit/ on GitHub Pages.
VITE_BASE_PATH=./
